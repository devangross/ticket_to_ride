package agent;

/**
 * Interface to represent a Referee as an agent that makes actions in the game.
 * Specifically, the referee deals with the actions that progress the game,
 * enforce rules, and end the game. Constructed by the Tournament manager. In
 * each game there will be a single instance of Referee which will interact
 * independently and sequentially with a number of different player instances
 * (for the current assignment [2,8] players).
 */
public interface IReferee {

    // public interface methods to be determined by remote interactions

}
